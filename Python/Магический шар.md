```python
import random
answer = ['Да.', 'Нет.', 'Боги думают над этим вопросом...']

print("Введите exit для выхода.\n")
while True:
    x = input("Боги ждут ваш вопрос!\n"
            "> ")
    if x.find("exit") != -1:
        print("До новых встреч!")
        break
    else:
        print(random.choice(answer))
        print()
```
